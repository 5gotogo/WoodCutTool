# WoodCutTool

Static website for [woodcuttool.com](https://woodcuttool.com/), deployed with Cloudflare Pages.

## Local development

```sh
npm run dev
```

The local server uses port `4175`.

## Shared site chrome

Top navigation and footer markup are rendered from `assets/site-chrome.js`. HTML pages should keep only the `data-site-header` and `data-site-footer` mount points. Run `npm run apply:nav-cta` after generating pages to normalize any generated HTML back to those shared mount points.

## Validation

```sh
npm run check
```

This checks that the sitemap entries have matching local pages, every indexable `index.html` page is listed in `sitemap.xml`, and internal links point to existing routes or files.

## Sitemap generation

```sh
npm run sitemap
```

The sitemap is generated by scanning every indexable `index.html` page and reading its canonical URL. Cloudflare Pages runs this automatically through `npm run build`, so new pages are added to `sitemap.xml` during deployment.

## Cloudflare Pages

Use these settings when connecting the GitHub repository:

- Framework preset: `None`
- Build command: `npm run build`
- Build output directory: `.`
- Production branch: `master`
- Custom domain: `woodcuttool.com`

The repository includes:

- `_headers` for security and cache headers.
- `_redirects` for `www` to apex domain and canonical `index.html` redirects.
- `wrangler.toml` for direct `wrangler pages deploy` compatibility.
