# WoodCutTool SEO Strategy & Implementation Log

This doc records (1) the diagnosis of the site's SEO, (2) what was implemented in code
for priorities P2–P3, and (3) the operational P4 actions that cannot be done in code and
need a human (backlinks, E-E-A-T, content focus).

---

## Diagnosis (priorities)

| Priority | Issue | Status |
|----------|-------|--------|
| **P1** | **Topic dilution** — the 970-post blog and 30+ app catalog span woodworking plus unrelated product topics (QuiltFit, Tinnitus, PDF scanning, and others). This can dilute woodworking topical focus. | ⏳ **Needs owner decision** (see below) |
| **P2** | Technical SEO gaps: zero Open Graph/Twitter tags, only 1 page with BreadcrumbList, no HowTo schema, no social share image. | ✅ **Done in code** |
| **P3** | Thin long-tail tool/template coverage. | ✅ Expanded to 60 template pages plus Learn, comparison, glossary, and wood-species clusters |
| **P4** | Weak trust transparency and external authority. | ✅ About/editorial process and visible authorship added; backlinks remain operational |
| **P5** | Scaled-content quality risk across a large generated site. | 🔁 Enforced with source generators, practical caveats, descriptive links, and automated audits |

---

## P2 — Technical SEO (implemented)

All generated by build scripts so they stay consistent and survive rebuilds.

### Open Graph + Twitter Cards — site-wide
- New shared module [`scripts/seo-meta.mjs`](../scripts/seo-meta.mjs) emits `og:*` and
  `twitter:*` tags from each page's title/description/canonical.
- Wired into every generator: blog, learn, apps, and the hand-written pages
  (via [`scripts/apply-seo-meta.mjs`](../scripts/apply-seo-meta.mjs)).
- Blog + learn articles use `og:type=article`; everything else `website`.

### BreadcrumbList structured data
- Added to learn articles, learn landing pages, blog posts (in their generators) and to
  hand-written tool/template pages (via `apply-seo-meta.mjs`, injected after `<body>` so
  it never collides with the SoftwareApplication JSON-LD that `apply-tool-schemas.mjs`
  replaces). Coverage went from 1 page → 80+ pages.
- Gives Google the breadcrumb trail it can show in results (CTR lift).

### HowTo structured data
- Added to the 6 calculators (plywood, cut-list, wood-waste, board-foot, stair-stringer,
  tile) in [`scripts/apply-tool-schemas.mjs`](../scripts/apply-tool-schemas.mjs) via a new
  `howto` field + `howToSchema()`. Each is a 4-step "how to use this calculator". Google
  deprecated HowTo rich results in 2023, so this markup is retained for machine-readable
  semantics and must not be treated as a current rich-result opportunity.

### Social share image
- [`assets/og/woodcuttool-og.png`](../assets/og/woodcuttool-og.png) — 1200×630, brand
  colors, generated from [`woodcuttool-og.svg`](../assets/og/woodcuttool-og.svg) (kept as
  the editable source). Referenced as the default `og:image` everywhere.
- **TODO (nice-to-have):** per-page OG images (e.g. one per calculator) would lift social
  CTR further. Current setup uses one shared image, which is fine to ship.

### Build order
`package.json` `build` now runs: apps → blog → learn → tool-schemas → **apply-seo-meta →
add-tool-guide-links** → sitemap → validate. `apply-seo-meta` runs *after* tool-schemas
on purpose (breadcrumb vs SoftwareApplication JSON-LD ordering).

---

## P3 — Long-tail content (implemented)

Two new project-specific template pages targeting clear search intent, each converting to
the plywood/cut-list calculators and CutList app:

- [`/templates/kitchen-cabinet-cut-list/`](../templates/kitchen-cabinet-cut-list/) —
  "kitchen cabinet cut list" (base + wall cabinet parts tables).
- [`/templates/workbench-cut-list/`](../templates/workbench-cut-list/) —
  "workbench cut list" (DIY plywood bench).

Both are interlinked with the existing `/templates/4x8-plywood-sheet/` page and pulled in
from the `how-many-sheets-of-plywood-do-i-need` learn article, so they are not orphans.
The sitemap auto-discovers them.

The generator-backed template library has since expanded to 60 pages. Future work should
improve the usefulness and internal-link context of existing pages before adding more URLs.
Google's scaled-content policy makes page value — not page count — the controlling metric.

---

## P4 — Trust and editorial transparency (implemented 2026-07-16)

- Added [`/about/`](../about/) with the site's purpose, editorial method, automation
  disclosure, reference limitations, correction path, and contact information.
- Added visible, descriptive author links to generated blog, Learn, comparison, app
  comparison, wood-species, and hand-written article pages.
- Updated Article and BlogPosting authors to `WoodCutTool Editorial Team` with `/about/`
  as the author URL, while keeping WoodCutTool as publisher.
- Expanded generated template pages with a visible preparation note, adjustable-template
  caveat, and a concrete dimension/hardware/site verification workflow.
- Extended [`scripts/audit-seo.mjs`](../scripts/audit-seo.mjs) so future Article or
  BlogPosting pages fail the audit if they have no visible author link.

The 2026-07-16 validation covered 1,883 HTML files and 1,860 sitemap URLs; the SEO audit
finished with no findings.

## P5 — Original dataset expansion (implemented 2026-07-17)

The research catalog now favors a small number of substantial canonical reports over
large batches of near-duplicate landing pages:

- Expanded the shared project benchmark from 24 to 36 source-linked parts lists.
- Added a 504-row project kerf sensitivity matrix: 36 projects × 7 kerf settings ×
  2 orientation modes.
- Added a 288-row sheet-format comparison: 36 projects × 4 nominal formats ×
  2 orientation modes, with explicit rejected-part and complete-layout fields.
- Added a 360-row edge-trim matrix: 36 projects × 5 per-edge margins ×
  2 orientation modes.

The three new reports expose 1,152 scenario rows as downloadable CSV files and use
canonical Dataset pages, DataDownload metadata, a DataCatalog hub, stable method/version
fields, visible limitations, source-template URLs, and a CC BY 4.0 license. The data is
computed from WoodCutTool's own planning examples rather than scraped plans, product
claims, customer files, or syndicated text.

This is the preferred pattern for future scale: add reproducible original evidence and
useful comparisons, not keyword-swapped pages. A low sheet count is never presented as a
successful format comparison when pieces were rejected, and every report states that the
heuristic is not a guaranteed optimum, construction drawing, purchase quantity, or
shop-ready cutting sequence.

### Future long-tail ideas (not yet built)
- `/templates/closet-organizer-cut-list/`, `/templates/bookshelf-cut-list/`
- `/templates/dresser-drawer-cut-list/`
- Calculator variant landing pages: "4x8 plywood cost calculator", "deck stair calculator".
- Only build these if there is real search demand; don't mass-produce thin pages.

---

## P1 — Topic dilution (needs an owner decision)

The single biggest structural lever. `woodcuttool.com` ranks for woodworking, but the
site also hosts a large unrelated app catalog. Pick one path:

- **A. Separate the unrelated apps** (recommended) — move non-woodworking app/blog content
  to a subdomain or separate site, or `noindex` + remove from the main sitemap. Makes
  woodcuttool.com ~100% woodworking and concentrates topical authority.
- **B. Internal-link isolation** — keep pages but split the tools hub into "Woodworking
  tools" and "Other apps", and stop linking the off-topic apps from the woodworking cluster.
- **C. Accept it** — if the app pages drive meaningful installs/revenue, keep as-is.

No code change made here — this is a strategy call. Once decided, it's straightforward to
implement (sitemap filtering, robots/meta, hub restructuring).

---

## External authority (operational — for a human)

These move rankings but can't be done in the repo. Rough priority order:

1. **Earn backlinks from woodworking communities.**
   - Participate genuinely on r/woodworking, woodworking forums, Facebook groups; link the
     free calculators when actually relevant (don't spam).
   - Submit to tool directories / "free woodworking tools" roundups.
   - Reach out to woodworking bloggers/YouTubers — a free, no-login plywood optimizer is an
     easy mention.

2. **Build E-E-A-T signals.**
   - Add a real **About** page: who builds this, woodworking background, why trust the math.
   - Add author bylines + short bios on learn/blog articles.
   - Show the methodology behind the calculators (how kerf/waste are computed) — first-hand
     expertise is what Google's "Experience" axis rewards.

3. **Publish one citable, data-driven asset.**
   - e.g. "We analyzed N cabinet cut plans — average plywood waste was X%." Original data
     attracts links far better than another how-to.

4. **Refocus content production on woodworking** (ties to P1) — stop splitting blog effort
   across unrelated apps; double down on the cutlist/plywood/cabinet topics that already
   perform.

5. **Monitor & maintain.**
   - Google Search Console: watch impressions/CTR per page; iterate titles where CTR lags
     (the title A/B variants are in [`docs/seo-titles-descriptions.md`](seo-titles-descriptions.md)).
   - Validate structured data in Google's Rich Results Test after deploy.
   - Keep Core Web Vitals healthy: the site is already lean (homepage ~17 KB, external CSS,
     no images on tool pages) — **don't add heavy JS or large hero images.**

---

## Quick reference — what to run

```bash
npm run build   # regenerate everything + validate (135 pages)
npm run check   # validate only
```

Editing meta: hand-written pages → `scripts/apply-seo-meta.mjs`; learn → its data in
`scripts/build-learn-content.mjs`; OG/breadcrumb helpers → `scripts/seo-meta.mjs`.
