import {mkdirSync,writeFileSync} from 'node:fs';
import {estimatingProjects as projects,estimatingGuides as guides} from './estimating-data.mjs';
import {ogTags} from './seo-meta.mjs';
import {cutlistConversionCta} from './conversion-components.mjs';
const base='/estimating/';
const esc=s=>String(s).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('"','&quot;');
const card=p=>`<article class="card"><p class="eyebrow">${p.tag}</p><h3>${p.name}</h3><p>${p.intro}</p><a class="card-link" href="${base}${p.slug}/">Estimate ${p.name.toLowerCase()} panels →</a></article>`;
const cta=()=>cutlistConversionCta({context:'result',source:'estimating-result',title:'Turn the allowance into a checked cut plan',description:'Recreate your confirmed parts in CutList on iPhone or iPad. Review sheet layouts, grain direction and the cutting sequence. Pro adds on-device AI Scan, PDF export, AirPrint and unlimited projects.',primaryLabel:'Get CutList for the next step',proof:['Free basic layouts','Pro features require a subscription','No login','Works offline']});
const field=(name,label,value,min=0,step='any',max='')=>`<label><span>${label}</span><input name="${name}" type="number" value="${value}" min="${min}" step="${step}" ${max?`max="${max}"`:''} required></label>`;
function calculator(p) {return `<section class="section" id="estimate"><div class="section-heading"><p class="eyebrow">One material · one repeated part</p><h2>Estimate ${p.part.toLowerCase()} stock and cost</h2><p>Editable demonstration: ${p.qty} ${p.part.toLowerCase()} pieces, ${p.length} × ${p.width} mm. The sheet price is illustrative, not a supplier quote. Replace every value with your own measurements.</p></div><div class="estimate-workspace"><form id="panel-estimate-form" class="estimate-form"><fieldset><legend>1. Finished panel</legend><div class="estimate-fields"><label>Dimension unit<select name="unit"><option value="mm">Millimeters</option><option value="in">Inches</option></select></label>${field('length','Part length (<span data-unit-label>mm</span>)',p.length,0.000001)}${field('width','Part width (<span data-unit-label>mm</span>)',p.width,0.000001)}${field('qty','Quantity',p.qty,1,1,10000)}</div></fieldset><fieldset><legend>2. Sheet stock</legend><div class="estimate-fields">${field('stockLength','Sheet length (<span data-unit-label>mm</span>)',p.stockLength,0.000001)}${field('stockWidth','Sheet width (<span data-unit-label>mm</span>)',p.stockWidth,0.000001)}${field('allowance','Added purchasing allowance (%)',10,0,'any',100)}${field('price','Price per whole sheet',p.price)}<label>Currency<select name="currency">${['USD','CAD','GBP','EUR','AUD','CNY'].map(c=>`<option>${c}</option>`).join('')}</select></label></div><label class="estimate-checkbox"><input type="checkbox" name="rotate"> Allow a 90° part rotation for the fit check</label><p>Leave unchecked when grain must follow part length. Kerf and trim are checked in the later layout, not this area estimate.</p></fieldset><fieldset><legend>3. Optional labor allowance</legend><div class="estimate-fields">${field('hours','Estimated hours',0)}${field('rate','Cost per hour',0)}</div></fieldset><p data-estimate-error role="alert" tabindex="-1"></p><button class="button" type="submit">Calculate panel allowance</button><p>No login. Inputs are used in this page and are not saved as a project.</p></form><aside class="estimate-notes"><p class="eyebrow">Scope of this estimate</p><h3>${p.part} panels only</h3><p>${p.excluded}</p><h3>How the number is calculated</h3><p>Length × width × quantity gives net area. Add your allowance, divide by sheet area, then round up to whole sheets. Multiply by your sheet price and add hours × hourly cost.</p><p><strong>A sheet count based on area is a minimum allowance, not a cut plan.</strong> It cannot prove that every part will pack.</p><a href="#next-step">What to do after calculating ↓</a></aside></div><div class="estimate-result" data-estimate-result role="region" aria-label="Panel estimate result" hidden></div><div data-estimate-next hidden>${cta()}<p class="estimate-transfer-note">The App Store link does not transfer these inputs. Enter and review your parts in CutList; a paper list can be scanned using Pro AI Scan.</p></div><noscript><p>Enable JavaScript for the editable estimate. You can still use the formula, sample dimensions and planning checklist on this page.</p></noscript></section>`;}
function related(p){if(!p.related)return '';const stair=p.related==='stairs';return `<section class="section"><div class="card"><p class="eyebrow">${stair?'If the job also includes a stair':'If the job also includes tile'}</p><h2>${stair?'Keep stair geometry in Stringer':'Keep the tile plan in TileFit'}</h2><p>${stair?'Use Stringer for riser options, stair geometry, stringer diagrams and a stair cut-sheet PDF. The rectangular storage allowance above does not calculate a stair or verify its structure.':'Use TileFit for floor or backsplash layouts, tile and box quantities, grouped cuts and a PDF installation packet. Tile area and box costs are separate from the cabinet sheet estimate.'}</p><a class="button secondary" href="/apps/${stair?'stringer':'tilefit-tile-layout-planner'}/">See ${stair?'Stringer':'TileFit'} for this part of the job</a></div></section>`;}
function page(title,description,path,body,interactive=false){const canonical=`https://woodcuttool.com${path}`; const schema={'@context':'https://schema.org','@type':'CollectionPage',name:title,description,url:canonical};return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${title} | WoodCutTool</title><meta name="description" content="${esc(description)}"><meta name="robots" content="index,follow"><link rel="canonical" href="${canonical}">${ogTags({title,description,canonical})}<link rel="icon" href="/favicon.ico"><link rel="stylesheet" href="/assets/content.css"><link rel="stylesheet" href="/assets/estimating.css"><script defer src="/assets/site-chrome.js"></script><script defer src="/assets/content-page.js"></script><script defer src="/assets/conversion.js"></script>${interactive?'<script type="module" src="/assets/estimating.js"></script>':''}<script type="application/ld+json">${JSON.stringify(schema)}</script></head><body><a class="skip-link" href="#main">Skip to content</a><div data-site-header></div><main id="main" class="estimating-page">${body}</main><div data-site-footer></div></body></html>`;}
function write(path,html){mkdirSync(new URL(`..${path}`,import.meta.url),{recursive:true});writeFileSync(new URL(`..${path}index.html`,import.meta.url),html);}
const hero=(title,intro,tag='Material → allowance → cut plan')=>`<section class="page-hero visual-hub-hero"><div><p class="breadcrumb"><a href="/">Home</a> / <a href="/tools/woodworking/">Woodworking</a> / <a href="${base}">Estimating</a></p><p class="eyebrow">${tag}</p><h1>${title}</h1><p class="lead">${intro}</p><div class="cta-row"><a class="button" href="#${title==='Woodworking material & cost planning'?'projects':'estimate'}">Start an estimate</a><a class="button secondary" href="/apps/cutlist/">Meet CutList</a></div></div><figure class="visual-frame"><img src="/assets/images/tools/tools-cut-layout-800.webp" alt="Sheet material and measured parts prepared for a cutting layout" width="800" height="600" fetchpriority="high"></figure></section>`;
const hubBody=hero('Woodworking material & cost planning','Get a useful panel allowance before you order. Choose a job, adjust the dimensions and your own prices, then take the confirmed parts into CutList for a reviewed layout and shop workflow.')+`<section class="section estimate-path"><article><span>01 / Measure</span><h2>Define the part</h2><p>Keep thickness, finish and grain consistent within each stock group.</p></article><article><span>02 / Budget</span><h2>Check the allowance</h2><p>See panel area, whole-sheet rounding and a partial cost subtotal.</p></article><article><span>03 / CutList</span><h2>Plan the actual cuts</h2><p>Review layouts and cutting order. Use Pro for scan, PDF and printing.</p></article></section><section class="section" id="projects"><div class="section-heading"><p class="eyebrow">Choose your job</p><h2>Six practical starting points</h2><p>Each estimate covers a named panel group. Follow its scope notes before expanding to the whole build.</p></div><div class="grid tools">${projects.map(card).join('')}</div></section><section class="section" id="planning-guide"><div class="section-heading"><p class="eyebrow">The estimating desk reference</p><h2>Eight checks between a measurement and an order</h2><p>Use these checks alongside every estimate. Open a focused existing tool when the job needs more detail.</p></div><div class="estimate-guides">${guides.map(([id,title,body,href,label],i)=>`<article id="${id}"><p class="eyebrow">Check ${String(i+1).padStart(2,'0')}</p><h3>${title}</h3><p>${body}</p><a href="${href}">${label} →</a></article>`).join('')}</div></section><section class="section"><div class="estimate-app"><div><p class="eyebrow">From an estimate to the saw</p><h2>One checked plan, ready for the workshop</h2><p>CutList creates sheet layouts from your actual stock and parts, with grain direction and kerf controls. Keep revisions in project history and follow the cutting sequence beside the material.</p><p>Basic layouts are free. AI Scan, PDF export, AirPrint and unlimited projects require Pro. Subscription pricing is shown in the app.</p><p>This website provides a single-use estimate. Re-enter reviewed inputs in the app; there is no automatic transfer.</p></div><img src="/assets/images/apps/cutlist/cutlist-cut-sequence.webp" width="420" height="900" loading="lazy" alt="CutList cutting sequence on iPhone"></div>${cta()}</section>`;
write(base,page('Woodworking material & cost planning','Estimate woodworking panel quantities, sheet allowances and partial material costs across six practical jobs, then plan actual cuts in CutList.',base,hubBody));
for(const p of projects){const title=`${p.name} panel cost planner`;write(`${base}${p.slug}/`,page(title,`Estimate ${p.name.toLowerCase()} ${p.part.toLowerCase()} panels with editable dimensions, whole-sheet allowances and your own material prices. Review the next steps in CutList.`,`${base}${p.slug}/`,hero(title,p.intro,p.tag)+calculator(p)+`<section class="section"><h2>Before measuring this panel group</h2><ol>${p.checks.map(c=>`<li>${c}</li>`).join('')}</ol><p><strong>Not included:</strong> ${p.excluded}</p><a href="${p.tool}">Review the related parts or layout tool →</a></section><section class="section" id="next-step"><h2>Build the checked parts list in CutList</h2><p>${p.next}</p><p>Confirm actual stock thickness, sheet dimensions, saw kerf, usable edges and permitted rotation. An allowance cannot establish a cutting order. Review the generated layout before ordering material, and keep the approved project available in the workshop.</p><a class="button secondary" href="/apps/cutlist/">See layouts, cutting sequence and Pro exports</a></section>${related(p)}<section class="section"><h2>Keep the estimate consistent</h2><div class="related-grid">${guides.filter(([id])=>['allowance','rounding','fit','labor'].includes(id)).map(([id,title])=>`<a href="${base}#${id}"><strong>${title}</strong></a>`).join('')}</div></section><section class="section"><h2>Estimate another job</h2><div class="grid tools">${projects.filter(x=>x.slug!==p.slug).slice(0,3).map(card).join('')}</div></section>`,true));}
console.log('Generated estimating hub, 6 scenario planners and 8 integrated planning guides.');
